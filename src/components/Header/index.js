import React from 'react';
import Nav from '../Nav';

function Header() {
    return (
        <section>
            <Nav></Nav>
            <h1>First Load</h1>
        </section>
    );
}

export default Header;